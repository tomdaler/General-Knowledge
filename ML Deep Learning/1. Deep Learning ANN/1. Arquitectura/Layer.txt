data augmented

en el primero, agregar data augmented y el data validation


kernel 3x3

padding = 'valid', 'same'  con same aumenta una linea de 0's alrededor
           para que punto (1,1) no pase solo 1 vez

strides = (1,1) se mueve 1 espacio horizontal y 1 vertical
          imagen no se reduce
          2 se va reduciendo la imagen resultante

MaxPooling2D(pool_size=(2,2)
            Con mas filtros en cada capa, se hace mas compleja con muchos parametros
            REDUCE COMPLEJIDAD
            ES OTRO KERNEL, OTRA MATRIZ
            Saca el maximo y el promedio del kernel de 2x2
            Reduce imagen
            Se esta quitando ruido y toma el pixel que mejor representa
            MAS COMUN EL MAX QUE EL AVG

            El Padding = 'same' aumenta linea alrededor
            El MaxPooling reduce la linea alrededor

RESUMEN

padding = 'same' aumenta linea alrededor para que posicion 1,1 puede ser procesada mas de 1 vez

Luego se aplica lo contrario con MaxPooling
- Reduce en imagen la linea alrededor
- Ademas que elimina el ruido
- El mas comun es el parametro MAX

PUEDE CAMBIARSE DE MAX A AVG

PARAMETROS = FILTROS X TAMANO KERNEL X PROFUNDIDAD DE CAPA ANTERIOR + FILROS(BIAS)


kernel_regularizer =
filtros  

BatchNormalization

Activation

Dropout

Flatten -> de matriz a vector

