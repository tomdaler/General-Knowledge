Clasificacion binario, la ultima capa se activa como signoide


from keras.layers import Dense, Input
from keras.models import Model


model = Sequential()
model.add(Dense(12, input_dim=8, kernel_initializer='uniform', activation='relu'))
model.add(Dense(8, kernel_initializer='uniform', activation='relu'))
model.add(Dense(1, kernel_initializer='uniform', activation='sigmoid'))

= = = = = = 

model = Sequential()
model.add(Dense(12, input_dim=8, activation='relu'))
model.add(Dense(8, activation='relu'))
model.add(Dense(1, activation='sigmoid'))

= = = = = 

model = Sequential([
     Dense(12, input_dim=8, activation='relu'),
     Dense(8, kernel_initializer='uniform', activation='relu'),
     Dense(1, kernel_initializer='uniform', activation='sigmoid')
     ])

= = = = = =

from keras.layers import Dense, Input
from keras.models import Model

I = Input(shape=(8,))
L = Dense(until = 12,activation = 'relu')(I)
L = Dense(until = 12,activation = 'relu')(L)

salida = Dense(until = 1,activation = 'sigmoid')(L)
model = Model(inputs = I, outputs=salida)

= = = = = = = 

from keras.layers import Dense, Input
from keras.models import Model

I = Input(shape=(8,))

L = Dense(until = 12)(I)
L = Activation('relu')(L)

L = Dense(until = 8)(L)
L = Activation('relu')(L)

L = Dense(until = 1)(L)
salida = Activation('sigmoid')(L)

model = Model(inputs = I, outputs=salida)

= = = = = 

from keras.layers import Dense, Input
from keras.models import Model

I = Input(shape=(8,))

densa12 = Dense(12,activation='relu')
densa8 = Dense(8,activation='relu')
salida = Dense(1,activation='sigmoid')

predicciones = salida(densa8(densa12(I)))
model = Model(inputs = I, outputs=predicciones)



